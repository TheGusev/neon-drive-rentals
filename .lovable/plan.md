## Проблема

1. На главной (мобилка) кнопка меню в правом верхнем углу срезается статус-баром/Dynamic Island — визуально «наполовину скрыта».
2. Ниже героя (блоки «RentSib — прокат…» и FAQ) идут в светлой теме, тогда как весь верх мобилки — тёмный неон. Пользователь хочет единую цветовую гамму на всей мобильной главной.

## Что поменяю

### 1. Кнопка меню — гарантированно ниже notch
`src/components/layout/PublicLayout.tsx`:
- В шапке добавить надёжный отступ сверху под notch: заменить `pt-5` на `pt-[max(env(safe-area-inset-top),1.25rem)]` плюс дополнительный `mt-2` на самой кнопке, чтобы она физически опустилась ниже статус-бара даже когда `safe-area-inset-top = 0` в preview.
- Оставить размер и неон-подсветку кнопки как есть.

(В `HomeStage.tsx` есть ещё второй, дублирующий hamburger внутри героя — на скрине его не видно, PublicLayout-шапка перекрывает; трогать не буду, чтобы не менять поведение других экранов.)

### 2. Единая тёмная гамма на всей мобильной главной
`src/routes/index.tsx`:
- Обёртку под героем `clean-light bg-background text-foreground` заменить на условие: на мобилке — `nfs-theme` (тёмный неон), на десктопе (`md:`) — прежняя светлая тема (десктоп ниже героя не скроллит, но правило `clean-light md:` сохраняем на всякий случай).

`src/components/home/FaqBlock.tsx`:
- Мелкая правка стилей триггеров/сепараторов, чтобы аккордеон читался на тёмном фоне (границы и hover через семантические токены `border-border/60`, `text-foreground`) — семантические токены уже переключатся автоматически, менять цвета жёстко не нужно; только проверю контраст заголовков `text-foreground`.

`src/components/home/HomeIntro.tsx` (проверю после чтения файла):
- Если использует жёсткие цвета — переведу на семантические `text-foreground` / `text-muted-foreground`, чтобы блок сам подхватил тёмную палитру.

Никаких изменений бизнес-логики, роутов, каталога, брони и админки.

## Проверка

- Открыть мобильный preview главной, убедиться что кнопка меню полностью видна ниже notch и что при скролле вниз (интро + FAQ) фон остаётся тёмным неоновым, текст читается.
- Десктоп главной не должен измениться визуально.
