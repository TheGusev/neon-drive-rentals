## Цель
Закрыть коммерческие и информационные запросы по аренде авто в Новосибирске: усилить главную (семантика, FAQ, JSON-LD), добавить 3 посадочные под ключи и блог из моков (MDX/JSON).

## Ключи по данным Semrush (ru)
Приоритет для главной и посадочных:
- «аренда авто новосибирск» — 3 600/мес, KDI 17 (легко) — H1 главной
- «прокат авто новосибирск» — 1 300/мес, KDI 28 — синоним в тексте + `/rent/novosibirsk`
- «аренда авто посуточно» — 480/мес — блок тарифов
- «аренда авто на сутки» — 720/мес — FAQ + текст
- «аренда авто с водителем» — 480/мес — упоминание в FAQ (отказ = уточнение)
- «каршеринг новосибирск» — 1 900/мес, KDI 36 — сравнительная статья в блоге (не конкурируем в лоб)
- Низкочастотные без Semrush-данных, но важные для нас: «без залога», «Толмачёво», «кей-кар», «японские авто» — целевые посадочные.

## 1. Главная страница — семантика и SEO-блоки
Файл: `src/routes/index.tsx`, компоненты в `src/components/home/`.

- **H1 (десктоп/мобилка)**: заменить «NSK-RENT» hero-подпись на семантичный H1 «Аренда авто в Новосибирске» (визуально NSK-RENT остаётся логотипом = `<p>`/`<span>`). Единственный H1 на странице.
- **Intro-текст** (300–400 знаков) под hero: вхождения «аренда/прокат авто в Новосибирске», «посуточно», «без залога», «японские кей-кары», «доставка в Толмачёво». Скрывать на десктопе нельзя — вписать в существующий стек (под `SeoTiles`).
- **FAQ-блок** — новый компонент `src/components/home/FaqBlock.tsx`, аккордеон на shadcn `Accordion`, 12 вопросов:
  1. Сколько стоит аренда авто в Новосибирске?
  2. Нужен ли залог?
  3. С какого возраста и стажа сдаёте?
  4. Какие документы нужны?
  5. Есть ли доставка в аэропорт Толмачёво?
  6. Можно ли выезжать за пределы НСО?
  7. Есть ли лимит пробега?
  8. Что входит в стоимость (страховка, ТО)?
  9. Как оплатить (карта/наличные/ЮKassa)?
  10. Что делать при ДТП или поломке?
  11. Можно ли продлить аренду?
  12. Чем кей-кары отличаются от обычных авто?
- **JSON-LD** в `head()` главной: `FAQPage` (все 12 Q/A) + `LocalBusiness` (`AutoRental`, адрес, телефон, geo, `areaServed: Новосибирск`, `priceRange: ₽₽`) + `BreadcrumbList`.
- **Breadcrumbs** визуальные — не нужны на главной, но JSON-LD добавим на посадочные.
- **Внутренняя перелинковка**: `SeoTiles` уже ведёт на `/cars` — переориентируем 3 плитки на новые `/rent/*` посадочные.

## 2. Три посадочные страницы
Каждая — отдельный маршрут, свой `head()` с уникальными title/description/og, свой H1, 400–600 знаков текста, JSON-LD `Service` + `BreadcrumbList`, CTA на `/cars` и `QuickBookingWidget`.

- `src/routes/_public.rent.novosibirsk.tsx` — «Аренда авто в Новосибирске: посуточно и на длительный срок»
- `src/routes/_public.rent.bez-zaloga.tsx` — «Аренда авто без залога в Новосибирске»
- `src/routes/_public.kei-cars.tsx` — «Кей-кары из Японии в аренду»

Общий шаблон в `src/components/seo/LandingPage.tsx` (hero → 2–3 текстовых секции → карточки авто из `cars.filter(...)` → мини-FAQ 4 вопроса → CTA).

## 3. Блог (моки в репо)
- `src/content/blog/*.mdx` — 6 статей + фронтматтер (title, description, slug, date, cover, keywords):
  1. Как арендовать авто в Новосибирске: пошаговая инструкция
  2. Аренда vs каршеринг в Новосибирске — что выгоднее
  3. Кей-кары: почему японские мини-авто идеальны для города
  4. Аренда авто в Толмачёво: как встретить и вернуть машину
  5. Что нужно для аренды без залога
  6. Поездка по НСО на арендованном авто: маршруты и правила
- Vite-загрузка через `import.meta.glob('/src/content/blog/*.mdx', { eager: true })` + `@mdx-js/rollup` уже нет в проекте → используем **frontmatter + Markdown-строку в JSON** для простоты: `src/mocks/blog.ts` (массив `BlogPost` с полем `body: string`), рендер через `react-markdown` (добавить пакет).
- Маршруты:
  - `src/routes/_public.blog.index.tsx` — список статей, `head()` с title/description, JSON-LD `Blog`.
  - `src/routes/_public.blog.$slug.tsx` — статья, `head()` из фронтматтера, JSON-LD `Article` + `BreadcrumbList`, og:image из cover.
- Ссылка на блог в главном меню (`PublicLayout` nav) и в футере.

## 4. Технические SEO-хвосты
- `public/sitemap.xml` уже нет / не проверял — создам `src/routes/sitemap[.]xml.ts` (server route) с базой `https://neon-drive-rental.lovable.app`, перечислением: `/`, `/cars`, все `/cars/$carId` из `mocks/cars`, `/rent/novosibirsk`, `/rent/bez-zaloga`, `/kei-cars`, `/blog`, все `/blog/$slug`, `/profile`. Без `<lastmod>` (нет достоверного источника).
- `public/robots.txt` — проверю/создам с `Allow: /` и `Sitemap: https://neon-drive-rental.lovable.app/sitemap.xml`.
- Каждая новая посадочная и `/blog/*` — `<link rel="canonical">` на себя, `og:url` self-reference.
- Alt-тексты для car-картинок уже есть в `PopularModels`; проверю в новых компонентах.

## 5. Мелочи по существующей главной
- H2 «Популярные модели» оставляем, добавим H2 «Ответы на частые вопросы» над FAQ, H2 «Наши услуги» над `SeoTiles`.
- `PopularModels`: добавить микро-текст (1 строка) с ключом «прокат японских кей-каров».

## Технические детали
- Новые пакеты: `react-markdown`, `remark-gfm`.
- Никаких изменений в бизнес-логике брони/чекаута.
- Всё серверно-рендерится (TanStack Start SSR) — FAQ-аккордеон открывается на клиенте, но контент вопросов/ответов в DOM сразу (важно для индексации).
- JSON-LD инжектится через `scripts: [{ type: 'application/ld+json', children: JSON.stringify(...) }]` в `head()`.

## Что НЕ трогаю
- Дизайн-систему, темы, чекаут, админку, роутинг существующих страниц (кроме добавления ссылки на блог в nav).
- Не подключаю Lovable Cloud под блог — по вашему выбору моки.

## Порядок реализации
1. FAQ-блок + JSON-LD на главной, H1, intro-текст.
2. Три посадочные + общий `LandingPage` шаблон.
3. Блог: моки, список, страница статьи, ссылка в nav.
4. `sitemap.xml` + `robots.txt`.
5. Прогон SEO-сканера (`seo_chat--trigger_scan`) и правка находок.
